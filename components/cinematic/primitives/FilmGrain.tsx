"use client";

/** Subtle film grain overlay for cinematic texture */
export default function FilmGrain() {
  return (
    <div
      className="film-grain pointer-events-none fixed inset-0 z-[90]"
      aria-hidden
    />
  );
}
