/** @deprecated Use brand.ts — kept for backward compatibility */
export {
  brandLight,
  brandDark,
  typography,
  brandAssets,
  glassmorphism,
  tailwindBrand as brandColors,
  nativeWindBrand as nativeWindTheme,
  getBrandTokens,
  getLogoPath,
} from "./brand";

export const spacing = { luxuryRadius: 28 } as const;
